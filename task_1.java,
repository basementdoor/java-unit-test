@Test
public void checkIsAdultWhenAgeIsMoreThan18True() {
    Program program = new Program();
    boolean isAdult = program.checkIsAdult(19);
    boolean shouldBeTrue = true;
    assertEquals("Ожидалось true",shouldBeTrue, isAdult);
}
